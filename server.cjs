{/*import express from 'express';
import { createProxyMiddleware } from 'http-proxy-middleware';

const app = express();

app.use('/api', createProxyMiddleware({ 
  target: 'https://app.ticketmaster.com', // this is the API server's base URL
  changeOrigin: true,
  pathRewrite: {
    '^/api': '', // this rewrites the initial part of the path
  },
}));

app.listen(3000, () => {
  console.log('Proxy server running on http://localhost:5173')
});*/}
